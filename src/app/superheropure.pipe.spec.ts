import { SuperheropurePipe } from './superheropure.pipe';

describe('SuperheropurePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperheropurePipe();
    expect(pipe).toBeTruthy();
  });
});
